name: Deploy

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Push empty commit as repo owner to trigger Vercel
        run: |
          git config user.name "beto2402"
          git config user.email "adibeto72@gmail.com"
          git commit --allow-empty -m "chore: trigger deployment"
          git push
